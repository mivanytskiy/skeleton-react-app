import * as React from 'react';
import { AdminComponent } from '../../components';

class Admin extends React.Component {
    public render() {
        return (
            <AdminComponent />
        );
    }
}

export const AdminContainer = Admin;
